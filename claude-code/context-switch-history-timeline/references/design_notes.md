このSkillは、作業中のコンテキスト（タスクやファイル名など）の切り替えをローカルで履歴化し、後から時系列で振り返ることを目的としています。履歴はJSON形式で保存し、最大100件までの最新履歴をタイムライン形式で表示します。CLIから明示的に記録・表示を呼び出せる設計とし、外部送信やクラウド保存は一切行いません。履歴が多い場合もパフォーマンス低下しないよう、部分表示や全件表示の切替が可能です。